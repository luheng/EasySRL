package edu.uw.easysrl.util;

import java.util.List;

/**
 * Created by luheng on 11/2/15.
 */
public class StringUtils {
/*
    public static String join(String[] arr, String delimiter) {
        String result = "";
        for (int i = 0; i < arr.length; i++) {
            result += (i > 0 ? delimiter : "") + arr[i];
        }
        return result;
    }

    public static String join(List<String> arr, String delimiter) {
        String result = "";
        for (int i = 0; i < arr.size(); i++) {
            result += (i > 0 ? delimiter : "") + arr.get(i);
        }
        return result;
    }
*/
}
