package edu.uw.easysrl.qasrl.qg.util;

import java.util.Map;
import java.util.HashMap;
import java.util.Optional;

import edu.uw.easysrl.syntax.grammar.Category;

import com.google.common.collect.ImmutableList;
import com.google.common.collect.ImmutableMap;
import com.google.common.collect.ImmutableSet;

/**
 * Only for adverbials, i.e., functions into (S\NP)\(S\NP)
 */
public class Adverb /* extends Predication */ {
    // private void validate() {
    //     if(!getPredicateCategory().isFunctionInto(Category.valueOf("(S\\NP)\\(S\\NP)"))) {
    //         throw new IllegalArgumentException("adverbial predication must be over an adverbial");
    //     }
    // }
}
